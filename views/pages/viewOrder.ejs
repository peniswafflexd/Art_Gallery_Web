<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders</title>
    <%- include('../partials/head'); %>
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <link href="/css/viewOrder.css" rel="stylesheet" type="text/css">

</head>
<body>
<header>
    <%- include('../partials/nav', {user}); %>
</header>

<!--Need to add user session checking
    Along with the addition using the server.js and
    art objects to pull in the objects into the table -->

    <div class="container">
        <% if(!order){ %>
            <p>This is not a valid order</p>
        <% } else { %>
            <h2>Order</h2>
        <table>
            <thead>
            <tr>
                <th>Author</th>
                <th>Description</th>
                <th>Price</th>
                <th>Media</th>
            </tr>
            </thead>
            <tbody>

            <% for( let i = 0; i < order.artwork_array.length; i++) { %>
                <tr class="art-container">
                    <td><%= order.artwork_array[i].author %></td>
                    <td><%= order.artwork_array[i].description %></td>
                    <td>$<b id="elemPrice<%= i %>"><%= order.artwork_array[i].price %></b></td>
                    <td>
                        <div class="img-container<%= i % 2 %>"><img src="<%= order.artwork_array[i].media_url %>" alt="Artwork"/>
                        </div>
                    </td>
                </tr>
            <% } %>
            <tr>
                <td>Total Price:</td>
                <td/>
                <td>$<b id="totalPrice"><%= order.price %></b></td>
            </tr>
            </tbody>
        </table>
    </div>
<% } %>

</body>
</html>