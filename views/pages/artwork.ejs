<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('../partials/head'); %>
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <link href="/css/artwork.css" rel="stylesheet" type="text/css">
    <script>
        function test() {
            const xhttp = new XMLHttpRequest();
            xhttp.withCredentials = true;
            xhttp.open("DELETE", "/art/<%= currentArtwork.id %>", true);
            xhttp.onreadystatechange = () => {
                if (xhttp.readyState === 4) {
                    if (xhttp.response === "ok") {
                        window.location.href = '/'
                    }
                }
            }
            xhttp.send();

        }
    </script>

</head>
<body>
<header>
    <%- include('../partials/nav', {user}); %>
</header>
<script src="ejs.min.js"></script>

<div class="container">
    <img src="<%= currentArtwork.media_url %>" alt="Artwork"/>
    <div class="info-container">
        <div class="desc-container">
            <p><b>Author: </b><%= currentArtwork.author %>  </p>
            <p><b>Description: </b><%= currentArtwork.description %></p>
            <p><b>Price: $</b><%= currentArtwork.price %></p>
        </div>
        <div class="button-container">
            <% if(!currentArtwork.purchased) { %>
                <form class="button" action="/cart/<%= currentArtwork.id %>" method="post">
                    <Button name="cart" title="Add to cart" value="Add to cart">Add to cart</Button>
                </form>
            <% } else { %>
                <p>Sorry, this artwork has already been purchased</p>
            <% } %>

            <% if(admin) { %>
                <div class="button">
                    <Button title="Delete" value="Delete" onclick="test()">Delete</Button>
                </div>
            <% } %>
        </div>
    </div>
</div>
</body>
</html>